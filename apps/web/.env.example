# URL da API do Fisgou (futuro apps/api — Express + Prisma + PostgreSQL).
# Enquanto o back-end não existe, a web roda 100% com mock data.
NEXT_PUBLIC_API_URL=http://localhost:4000

# Chave do Google Maps / Places API (tela de pesqueiros).
# Hoje a tela usa um mapa placeholder; ao plugar a chave vira Places real.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
